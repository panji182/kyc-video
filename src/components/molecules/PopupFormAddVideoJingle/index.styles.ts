import { toEm, toRem } from '@/helpers/globalFunctions';

const styles = {
  textError: {
    marginBottom: toRem(8),
    marginLeft: toRem(8),
    color: '#FF1D1D',
    fontSize: toEm(18),
  },
};

export default styles;
